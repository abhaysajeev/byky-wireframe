# Development(Local) Env File
# ------------------------------------------------------------------------------

# ! Don't commit this file in real project if contains sensetive information

DEBUG=True
DJANGO_ENVIRONMENT="local"
SECRET_KEY = "replace-with-your-own-secret-key"

# Update this with the actual base URL of your application
BASE_URL = 'http://127.0.0.1:8000'
# Share the prototype with the team over the LAN. Set to this machine's LAN IP;
# comma-separate if it has more than one. Leave blank to keep it local-only.
LAN_HOSTS=192.168.0.66
